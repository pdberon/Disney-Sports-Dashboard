import '../styles/globals.css' // Ajusta esta ruta si tu archivo de estilos de Tailwind está en otra ubicación

export default function App({ Component, pageProps }) {
  return <Component {...pageProps} />
}
